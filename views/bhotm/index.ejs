<%- include ("../partials/header") %>
<div class="container">
    <h1 class="display-4 my-3">Ben Hagle of the Month</h1>
    <p class="lead">Ben Hagle of the Month is a monthly competetion to determine who is Ben Hagle. Submissions can be literally anything, creativity is encouraged. The winner is decided by the previous Ben Hagle. Submissions can be sent to <a href="mailto:hen.bagle@gmail.com">hen.bagle@gmail.com</a>, and are due on the 4th of each month, at midnight in your local timezone.</p>
    <h3 class="my-4 text-center"><strong>Entries Due: </strong><span id="bhotmCountdown"></span></h3>
    <h2 class="display-5 my-4">Previous submissions</h2>
    <div class="accordion" id="previousAccordion">
        <% for(i=0;i<bhotm.length;i++){%>
        <div class="card">
            <div class="card-header" id="heading<%=i%>">
                <h2 class="mb-0"><button class="btn btn-link" type="button" data-toggle="collapse" data-target="#bhotm<%=i%>"aria-expanded="true" aria-controls="collapseOne"><%=bhotm[i].month%></button></h2>
            </div>
            <div id="bhotm<%=i%>" class="collapse" aria-labelledby="heading<%=i%>" data-parent="#previousAccordion">
                <div class="card-body">
                    <div class="card-columns">
                    <% for(j=0;j<bhotm[i].submissions.length;j++){%>
                        <div class="card <% if (bhotm[i].submissions[j].isWinner){%>text-white bg-success<%}%> p-0">
                            <div class="card-header">
                                <a href="/bhotm/entry/<%=bhotm[i].submissions[j].id%>"><h5 class="card-title"><%=bhotm[i].submissions[j].place%>. <%=bhotm[i].submissions[j].name%></h5></a>
                                <% if(bhotm[i].submissions[j].entryName){%>
                                    <h6 class=""><%=bhotm[i].submissions[j].entryName%></h6>
                                <%}%>
                            </div>
                            <div class="card-body p-2">
                                <% if(bhotm[i].submissions[j].format == "image"){%>
                                    <a href="<% if(bhotm[i].submissions[j].clickLink){%>
                                                    <%=bhotm[i].submissions[j].clickLink%>
                                            <%} else{%>
                                                    <%=bhotm[i].submissions[j].link%>
                                             <%}%>"><img src="<%=bhotm[i].submissions[j].link%>" class="card-img-top bhotmi"></a>
                                <%}%>
                                <% if(bhotm[i].submissions[j].format == "link"){%>
                                    <a href="<%=bhotm[i].submissions[j].link%>"><h3 class="text-center">Link to Entry</h3></a>
                                <%}%>
                                <% if(bhotm[i].submissions[j].format == "phrase"){%>
                                    <h5 class="text-center">"<%=bhotm[i].submissions[j].link%>"</h5>
                                <%}%>
                                <% if(bhotm[i].submissions[j].format == "audio"){%>
                                    <div class="text-center m-1">
                                        <audio controls src="<%=bhotm[i].submissions[j].link%>"></audio>
                                    </div>
                                <%}%>
                                <% if(bhotm[i].submissions[j].format == "youtube"){%>
                                    <div class="text-center m-1">
                                        <iframe src="<%=bhotm[i].submissions[j].link%>" frameborder="0" width="100%" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
                                    </div>
                                <%}%>
                                <% if(bhotm[i].submissions[j].format == "mason"){%>
                                    <img src="/img/SmackSonUS1.jpg" class="card-img-top bhotmi">
                                    <p class="text-center">Did not submit, is Mason.</p>
                                <%}%>
                                <% if(bhotm[i].submissions[j].clickLink){%>
                                    <a href="<%=bhotm[i].submissions[j].clickLink%>"><h3 class="text-center m-2">Link to Full Entry</h3></a>
                                <%}%>
                                <% if(bhotm[i].submissions[j].entryDescription){%>
                                    <p class=""><%=bhotm[i].submissions[j].entryDescription%></p>
                                <%}%>
                            </div>
                        </div>
                    <%}%>
                    </div>
                    <h5 class="mx-auto"><%=bhotm[i].notes%></h5>
                </div>
            </div>
        </div>
        <%}%>
    </div>
</div>
<script src="https://cdnjs.cloudflare.com/ajax/libs/dayjs/1.8.35/dayjs.min.js" integrity="sha512-nG86d7fodOg4f3fZF796WwIj6WwxdWI2DCXCarXZU05/UNHzC2CHQkD6dNdj6Ahaurrbed66rsvTt6r58RQ5WA==" crossorigin="anonymous"></script>
<script src="js/bhotmCountdown.js"></script>
<%- include ("../partials/footer") %>